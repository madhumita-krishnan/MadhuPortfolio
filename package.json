{
  "name": "madhu-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Madhumita Krishnan — portfolio. Content lives in data/, build.js compiles it to static HTML in dist/.",
  "scripts": {
    "build": "node build.js",
    "start": "node server.js",
    "dev": "node build.js && node server.js"
  }
}
